<DOCTYPE HTML5>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <link rel="stylesheet" type="text/css" href="/css/login.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  </head>

  <body width="100%" height="100%">
    <form action="/login/login_process" method="post" class="loginForm" >            
      <h2>User Login</h2>
      <div class="idForm">
        <input type="text" class="id" id='id' name='id' placeholder="ID">
      </div>
      <div class="passForm">
        <input type="password" class="pw" id='pw' name='pw' placeholder="PW">
      </div>
      <input type="submit" class="btn" value="SIGN IN" onclick="checkPw()" >
      
      <div class="bottomText">
        Don't you have ID? 
        <span class='signup'><a href="./signup">회원가입</a></span>
      </div>
    </form>
     
     <!--  <script>
       let count = 0;
        function checkPw(){
          const id = $("#id").val();
          const pw = $("#pw").val();

          $.ajax({
                        type:"post",
                        url:"/",
                        data: {id:id,pw:pw},
                        dataType:'json',
                        async: false,
                        success: function(data){
                            if(data==='1'){
                            count=1
                                     }else{
                                    count=0;  
                                     }
                        },error:function(request,status,error){
                            alert("code = "+ request.status + " message = " + request.responseText + " error = " + error);
                        }
                     })    
        }
  
  
         function ch1(){
          if(count===0){
            alert(count)
            alert('아이디가 없거나 비밀번호가 틀립니다!')
            return false
          }else{
            alert('로그인!')
          }
        } 
  
  
  
      </script> -->



  </body>
</html>
